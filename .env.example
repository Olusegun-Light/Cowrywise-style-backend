NODE_ENV=development
PORT=3000

# Local Postgres (native install, trust auth — replace `backend` with your OS username if different)
DATABASE_URL="postgresql://backend@localhost:5432/cowrywise_dev?schema=public"

# Local Redis (native install)
REDIS_URL="redis://localhost:6379"
JWT_SECRET="replace-with-a-generated-64-char-hex-secret"
